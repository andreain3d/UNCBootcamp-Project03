import React, { Fragment } from "react";
import Profile from "../components/Profile";

const ProfileView = () => {
  return (
    <Fragment>
      <Profile />
    </Fragment>
  );
};

export default ProfileView;
